const Page<%= it.pageNum %> = () => {
    return (
        <div className="flex-grow flex items-center justify-center">
            <h1>This is a page</h1>
        </div>
    );
};

export default Page<%= it.pageNum %>;